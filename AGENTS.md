# 良いシーン切りぬく〜よ - Codex 開発ルール

このリポジトリで作業する際は、実装前に依頼内容に関係する仕様書を確認すること。

## プロジェクト取り違え防止

ファイルの作成・変更・削除、ビルド、テスト、Git操作など、プロジェクトへ影響する作業を始める前に、必ず対象プロジェクトを確認する。

### 正しいプロジェクト

- Project: `YoiSceneKirinuKu`
- Root: `/Users/kazuki/Documents/Codex/YoiSceneKirinuKu`

### 作業開始前の必須確認

1. 現在の作業ディレクトリが上記Rootと一致することを確認する。
2. リポジトリが `YoiSceneKirinuKu` であることを確認する。
3. 本 `AGENTS.md` を確認する。
4. 必要に応じて `docs/` 内の正本仕様書を確認する。
5. チャットの指示とリポジトリ内の正本が矛盾する場合は、推測で続行せず停止して報告する。

### 作業禁止条件

次のいずれかに該当する場合は、ファイル変更、コマンド実行、Git操作を開始しない。

- 作業ディレクトリが正しいRootと一致しない。
- 対象プロジェクトを一意に確認できない。
- ユーザーが貼り付けた内容が別プロジェクト由来である可能性がある。
- 指示されたファイルがこのリポジトリに存在せず、別プロジェクトのファイルである可能性がある。
- チャット上のプロジェクト名と実際のリポジトリが一致しない。

該当する場合は「プロジェクト取り違えの可能性あり」と報告し、ユーザーの確認を待つ。

### プロジェクト境界

- このリポジトリ外のファイルを変更しない。
- 別プロジェクトの仕様、コード、検証結果、Git履歴を、このプロジェクトのものとして扱わない。
- チャット履歴だけを根拠に、別プロジェクトの作業を継続しない。
- リポジトリ内の `AGENTS.md` と正本仕様書をプロジェクト識別の基準とする。

### 作業報告

重要な作業の開始時には、少なくともProject、Working directory、対象ファイルを確認する。Git操作を伴う場合は、必要に応じてHEADと作業ツリーの状態も確認する。

## 参照する仕様書

- 本体仕様: `docs/PRODUCT_SPEC.md`
- UI仕様: `docs/UI_SPEC.md`
- アーキテクチャ: `docs/ARCHITECTURE.md`
- 開発ルール: `docs/DEVELOPMENT_RULES.md`
- UIデザイン参考: `docs/design/DESIGN_REFERENCE.md`

仕様の優先順位は次の通り。

1. `docs/PRODUCT_SPEC.md`
2. `docs/UI_SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/DEVELOPMENT_RULES.md`
5. `docs/design/DESIGN_REFERENCE.md`
6. `reference-images/` の参考画像

仕様同士に矛盾がある場合や判断できない場合は、推測で機能を追加・変更せず確認すること。


## デザイン参考画像

UIを実装するときは、必要に応じて `docs/design/DESIGN_REFERENCE.md` と対応する `reference-images/` の画像を参照してよい。

ただし参考画像は視覚デザインの参考資料であり、機能仕様ではない。

- 参考画像と仕様書が矛盾する場合は必ず仕様書を優先する。
- 参考画像にだけ存在する機能を実装してはいけない。
- 画像のレイアウトをそのままコピーするのではなく、現行仕様に必要な要素だけ採用する。
- 特に、設定画面、品質モード、AI％表示、全体進捗％、残り時間、CPU/メモリ、一時停止、解析中プレビュー、波形、星評価などは画像にあっても初期版へ追加しない。

## 絶対ルール

- 仕様にない機能を勝手に追加しない。
- 元動画を書き換えない。
- `shell=True` を使わない。
- 外部コマンドは引数配列で渡す。
- partial方式を守り、正式完成前のデータを完成扱いしない。
- 「押した」「要求した」「プロセスからfinishedを受けた」だけで成功扱いしない。
- 正式成果物を検証してからUIを完了状態へ移す。
- stdoutはSwift-Python間のJSON通信専用にする。通常ログはstderrまたはログファイルへ出す。
- AIの生スコアを「本人確率」などとしてUIへ表示しない。
- 新しい依存ライブラリを勝手に追加しない。
- 依頼と無関係なリファクタリングをしない。
- 完成MP4をアプリから削除しない。
- 削除可能な作業領域のルートを固定する。
- 解析ジョブは同時に1件だけとする。
- SwiftUI Viewから重要なアプリ状態を直接変更しない。
- Viewの寿命とPythonプロセスの寿命を分離する。

## 作業単位

原則、1回の依頼につき1機能程度を扱う。

例:

- Home画面を作る
- MP4選択を追加する
- Preflightを実装する
- 人物選択を追加する
- Analysis画面を作る
- 解析停止状態を実装する
- Results画面を作る
- AVPlayerプレビューを追加する

一度にアプリ全体を実装しない。

## 実装前

1. この `AGENTS.md` を確認する。
2. 依頼内容に関連する `docs/` の仕様を読む。
3. 既存実装・テストを確認する。
4. 変更範囲を必要最小限にする。

## 実装後

少なくとも以下を確認する。

- ビルド成功
- 既存テスト成功
- 必要な新規テスト追加
- 主要なエラー状態の確認
- `git diff` の確認
- 仕様外機能を追加していないこと
- 無関係なファイルを変更していないこと

## 開発の合言葉

- 今必要なものだけ見せる。
- UIに嘘をつかせない。
- 押した＝成功、にはしない。
- 完成したものだけ✓を付ける。
- 解析と保存は別。
- AIの数字より、人間が判断しやすい表示。
- 設定を増やす前に、本当に必要か確認する。
- 便利なUIより、迷わないUI。
- Codexには一気に全部作らせない。
- まず正しく動く。そのあと美しく仕上げる。


## 参考画像の参照
UI実装時は `docs/design/DESIGN_REFERENCE.md` を確認すること。参考画像は機能仕様ではなく、上位仕様と矛盾する場合は必ず上位仕様を優先する。
